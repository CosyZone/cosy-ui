{
	"name": "@coffic/cosy-workspace",
	"type": "module",
	"scripts": {
		"clean": "pnpm -r clean",
		"build": "pnpm format && pnpm build:ui && pnpm build:docs",
		"build:ui": "pnpm --filter @coffic/cosy-ui build",
		"build:docs": "pnpm build:ui && pnpm --filter @coffic/cosy-docs build",
		"build:vue": "pnpm --filter vue-example build",
		"dev:docs": "pnpm build:ui && pnpm --filter @coffic/cosy-docs dev",
		"dev:vue": "pnpm --filter vue-example dev",
		"dev": "pnpm format && pnpm dev:docs",
		"lint": "biome lint --write ./",
		"preview:docs": "pnpm --filter @coffic/cosy-docs preview",
		"check": "pnpm --recursive run check",
		"format": "biome format --write ./",
		"deploy:docs:cloudflare": "pnpm --filter @coffic/cosy-docs deploy:cloudflare"
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@biomejs/biome": "^2.3.2",
		"typescript": "^5.9.3"
	},
	"engines": {
		"node": ">=23.0.0",
		"pnpm": ">=10.0.0"
	}
}
