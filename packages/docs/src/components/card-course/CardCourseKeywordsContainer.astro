---
/**
 * @component CardCourseKeywordsContainer
 * @description CardCourse 关键词功能演示容器，展示如何使用关键词来匹配更合适的图标。
 */
import { CodeContainer } from "@coffic/cosy-ui";
import CardCourseWithKeywords from "./CardCourseWithKeywords.astro";

import CardCourseWithKeywordsSource from "./CardCourseWithKeywords.astro?raw";

const codes = [CardCourseWithKeywordsSource];
const titles = ["使用关键词匹配图标"];
---

<CodeContainer codes={codes} titles={titles}>
  <div id="tab-1">
    <CardCourseWithKeywords />
  </div>
</CodeContainer>
