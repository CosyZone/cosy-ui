---
import { cn } from '../../src/class';

// 使用 class builder 构建各个部分的类名
const outerClass = cn().relative().w('full').h('full').build();

const innerClass = cn().absolute().inset(0).build();
---

<div class={outerClass}>
    <div class={innerClass}>
        <slot />
    </div>
</div>
