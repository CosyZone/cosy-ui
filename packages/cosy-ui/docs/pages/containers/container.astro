---
import Layout from '../../layouts/DemoLayout.astro';
import { Container as ContainerComponent, CodeExample } from '@/index';
---

<Layout title="Container 组件示例">
  <main class="container mx-auto p-4 space-y-4">
    <h1 class="text-2xl font-bold mb-6">Container 组件示例</h1>

    <div class="space-y-8">
      <!-- 基础用法 -->
      <CodeExample 
        title="基础用法" 
        description="最简单的容器用法，默认使用中等尺寸和中等内边距"
        code={`<Container>
  <div class="bg-gray-100 p-4 text-center">
    内容将被限制在一个合理的宽度内并居中显示
  </div>
</Container>`}
      >
        <ContainerComponent>
          <div class="bg-gray-100 p-4 text-center">
            内容将被限制在一个合理的宽度内并居中显示
          </div>
        </ContainerComponent>
      </CodeExample>

      <!-- 不同尺寸 -->
      <CodeExample 
        title="不同尺寸" 
        description="Container 组件支持六种不同的尺寸：xs、sm、md、lg、xl 和 full"
        code={`<Container size="xs">
  <div class="bg-gray-100 p-4 text-center">超小尺寸容器</div>
</Container>

<Container size="sm">
  <div class="bg-gray-100 p-4 text-center">小尺寸容器</div>
</Container>

<Container size="md">
  <div class="bg-gray-100 p-4 text-center">中等尺寸容器（默认）</div>
</Container>

<Container size="lg">
  <div class="bg-gray-100 p-4 text-center">大尺寸容器</div>
</Container>

<Container size="xl">
  <div class="bg-gray-100 p-4 text-center">超大尺寸容器</div>
</Container>

<Container size="full">
  <div class="bg-gray-100 p-4 text-center">全宽容器</div>
</Container>`}
      >
        <div class="space-y-4">
          <ContainerComponent size="xs">
            <div class="bg-gray-100 p-4 text-center">超小尺寸容器</div>
          </ContainerComponent>
          
          <ContainerComponent size="sm">
            <div class="bg-gray-100 p-4 text-center">小尺寸容器</div>
          </ContainerComponent>
          
          <ContainerComponent size="md">
            <div class="bg-gray-100 p-4 text-center">中等尺寸容器（默认）</div>
          </ContainerComponent>
          
          <ContainerComponent size="lg">
            <div class="bg-gray-100 p-4 text-center">大尺寸容器</div>
          </ContainerComponent>
          
          <ContainerComponent size="xl">
            <div class="bg-gray-100 p-4 text-center">超大尺寸容器</div>
          </ContainerComponent>
          
          <ContainerComponent size="full">
            <div class="bg-gray-100 p-4 text-center">全宽容器</div>
          </ContainerComponent>
        </div>
      </CodeExample>

      <!-- 不同内边距 -->
      <CodeExample 
        title="不同内边距" 
        description="Container 组件支持五种不同的内边距：none、sm、md、lg 和 xl"
        code={`<Container padding="none" class="mb-4">
  <div class="bg-gray-100 p-4 text-center">无内边距</div>
</Container>

<Container padding="sm" class="mb-4">
  <div class="bg-gray-100 p-4 text-center">小内边距</div>
</Container>

<Container padding="md" class="mb-4">
  <div class="bg-gray-100 p-4 text-center">中等内边距（默认）</div>
</Container>

<Container padding="lg" class="mb-4">
  <div class="bg-gray-100 p-4 text-center">大内边距</div>
</Container>

<Container padding="xl" class="mb-4">
  <div class="bg-gray-100 p-4 text-center">超大内边距</div>
</Container>`}
      >
        <div class="space-y-4">
          <ContainerComponent padding="none">
            <div class="bg-gray-100 p-4 text-center">无内边距</div>
          </ContainerComponent>
          
          <ContainerComponent padding="sm">
            <div class="bg-gray-100 p-4 text-center">小内边距</div>
          </ContainerComponent>
          
          <ContainerComponent padding="md">
            <div class="bg-gray-100 p-4 text-center">中等内边距（默认）</div>
          </ContainerComponent>
          
          <ContainerComponent padding="lg">
            <div class="bg-gray-100 p-4 text-center">大内边距</div>
          </ContainerComponent>
          
          <ContainerComponent padding="xl">
            <div class="bg-gray-100 p-4 text-center">超大内边距</div>
          </ContainerComponent>
        </div>
      </CodeExample>

      <!-- 居中选项 -->
      <CodeExample 
        title="居中选项" 
        description="通过 centered 属性控制容器是否居中显示"
        code={`<Container centered={true}>
  <div class="bg-gray-100 p-4 text-center">居中显示（默认）</div>
</Container>

<Container centered={false}>
  <div class="bg-gray-100 p-4 text-center">不居中显示</div>
</Container>`}
      >
        <div class="space-y-4">
          <ContainerComponent centered={true}>
            <div class="bg-gray-100 p-4 text-center">居中显示（默认）</div>
          </ContainerComponent>
          
          <ContainerComponent centered={false}>
            <div class="bg-gray-100 p-4 text-center">不居中显示</div>
          </ContainerComponent>
        </div>
      </CodeExample>

      <!-- 自定义样式 -->
      <CodeExample 
        title="自定义样式" 
        description="可以通过 class 属性添加自定义样式"
        code={`<Container class="bg-blue-50 rounded-lg shadow-md">
  <div class="p-4 text-center">
    带有自定义样式的容器
  </div>
</Container>`}
      >
        <ContainerComponent class="bg-blue-50 rounded-lg shadow-md">
          <div class="p-4 text-center">
            带有自定义样式的容器
          </div>
        </ContainerComponent>
      </CodeExample>

      <!-- 实际应用场景 -->
      <CodeExample 
        title="实际应用场景" 
        description="在实际应用中的常见用法"
        code={`<Container size="lg">
  <header class="py-4 border-b mb-6">
    <h2 class="text-xl font-bold">页面标题</h2>
  </header>
  
  <main class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <aside class="md:col-span-1 bg-gray-50 p-4 rounded">
      侧边栏内容
    </aside>
    
    <section class="md:col-span-2">
      <p class="mb-4">主要内容区域，使用容器组件可以确保内容在各种屏幕尺寸下都有合适的宽度和边距。</p>
      <p>容器组件是布局的基础，通常用于页面的最外层或主要内容区域。</p>
    </section>
  </main>
</Container>`}
      >
        <ContainerComponent size="lg">
          <header class="py-4 border-b mb-6">
            <h2 class="text-xl font-bold">页面标题</h2>
          </header>
          
          <main class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <aside class="md:col-span-1 bg-gray-50 p-4 rounded">
              侧边栏内容
            </aside>
            
            <section class="md:col-span-2">
              <p class="mb-4">主要内容区域，使用容器组件可以确保内容在各种屏幕尺寸下都有合适的宽度和边距。</p>
              <p>容器组件是布局的基础，通常用于页面的最外层或主要内容区域。</p>
            </section>
          </main>
        </ContainerComponent>
      </CodeExample>
    </div>
  </main>
</Layout> 
