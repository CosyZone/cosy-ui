---
import DemoLayout from '@docs/layouts/DemoLayout.astro';
import { Products, getExampleImage } from '@/index';
import { getBaseUrl } from '@docs/lib/utils';
---

<DemoLayout title="Cosy UI 组件库 - 示例" showSidebar={false}>
	<h1>Cosy UI 组件库 - 示例</h1>

	<Products
		products={[
			{
				name: '文档布局-宽屏模式',
				image: getExampleImage({
					width: 100,
					height: 100,
				}),
				description: '适用于展示文档的场景',
				productUrl: `${getBaseUrl()}/demos/doc_layout_full`,
			},
			{
				name: '文档布局-紧凑模式',
				image: getExampleImage({
					width: 100,
					height: 100,
				}),
				description: '适用于展示文档的场景',
				productUrl: `${getBaseUrl()}/demos/doc_layout_sm`,
			},
			{
				name: '文档布局-隐藏侧边栏',
				image: getExampleImage({
					width: 100,
					height: 100,
				}),
				description: '适用于展示文档的场景',
				productUrl: `${getBaseUrl()}/demos/doc_layout_hide_sidebar`,
			},
			{
				name: '文档布局-隐藏目录导航',
				image: getExampleImage({
					width: 100,
					height: 100,
				}),
				description: '适用于展示文档的场景',
				productUrl: `${getBaseUrl()}/demos/doc_layout_hide_toc`,
			},
		]}
	/>
</DemoLayout>
