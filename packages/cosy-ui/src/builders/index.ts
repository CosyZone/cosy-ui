/**
 * Props Builders - 内部工具
 *
 * 提供链式调用的方式来构建组件 props，统一管理组件属性。
 * 注意：这是 CosyUI 内部工具，不对外部用户暴露。
 */

export { PropsBuilder } from "./PropsBuilder";
export { AlertPropsBuilder, alertProps } from "../components/alert/AlertPropsBuilder";
