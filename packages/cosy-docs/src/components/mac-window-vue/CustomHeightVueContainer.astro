---
/**
 * @component MacWindow CustomHeight Vue Container
 * @description MacWindow 自定义高度 Vue 容器组件
 */

import { CodeContainer } from '@coffic/cosy-ui';
import CustomHeightVue from '../mac-window/CustomHeight.vue';
import CustomHeightVueSource from '../mac-window/CustomHeight.vue?raw';
---

<CodeContainer titles={['自定义高度']} codes={[CustomHeightVueSource]}>
  <div id="tab-1">
    <CustomHeightVue client:load />
  </div>
</CodeContainer>
