---

import { PictureBookPage, PictureBookTextBox } from "@coffic/cosy-ui";
---

<PictureBookPage background="primary/90">
    <PictureBookTextBox
        slot="overlay"
        top={28}
        left={10}
        width={80}
        bg="primary"
        paddingPx={10}
        roundedPx={10}
        zIndex={50}>
        zIndex=50（更高）
    </PictureBookTextBox>
</PictureBookPage>
