---
import { PictureBookPage, PictureBookTextBox } from '@coffic/cosy-ui';
---

<PictureBookPage background="primary/90">
    <PictureBookTextBox
        slot="overlay"
        top={30}
        left={12}
        width={76}
        bg="primary"
        paddingPx={10}
        roundedPx={10}
        zIndex={5}>
        zIndex=5（较低）
    </PictureBookTextBox>
</PictureBookPage>
