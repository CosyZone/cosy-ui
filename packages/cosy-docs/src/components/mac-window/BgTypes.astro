---
/**
 * @component MacWindow BgTypes Example
 * @description 展示 MacWindow 组件的不同背景色类型
 */

import { MacWindow } from '@coffic/cosy-ui';
import type { MacWindowBgType } from '@coffic/cosy-ui';
---

<div class="space-y-8">
  <!-- 默认背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">默认背景</h3>
    <MacWindow title="默认背景窗口" height="h-48">
      <div class="p-4">
        <p>这是使用默认背景色的 MacWindow 组件。</p>
        <p>背景色：浅色主题，适合大多数场景。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 主要背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">主要背景</h3>
    <MacWindow title="主要背景窗口" height="h-48" bgType="primary">
      <div class="p-4">
        <p>这是使用主要背景的 MacWindow 组件。</p>
        <p>背景色：主要主题背景，适合重要内容。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 次要背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">次要背景</h3>
    <MacWindow title="次要背景窗口" height="h-48" bgType="secondary">
      <div class="p-4">
        <p>这是使用次要背景的 MacWindow 组件。</p>
        <p>背景色：次要主题背景，适合辅助内容。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 强调背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">强调背景</h3>
    <MacWindow title="强调背景窗口" height="h-48" bgType="accent">
      <div class="p-4">
        <p>这是使用强调背景的 MacWindow 组件。</p>
        <p>背景色：强调主题背景，适合突出显示。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 中性背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">中性背景</h3>
    <MacWindow title="中性背景窗口" height="h-48" bgType="neutral">
      <div class="p-4">
        <p>这是使用中性背景的 MacWindow 组件。</p>
        <p>背景色：中性主题背景，适合通用内容。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 信息背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">信息背景</h3>
    <MacWindow title="信息背景窗口" height="h-48" bgType="info">
      <div class="p-4">
        <p>这是使用信息背景的 MacWindow 组件。</p>
        <p>背景色：信息主题背景，适合提示信息。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 成功背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">成功背景</h3>
    <MacWindow title="成功背景窗口" height="h-48" bgType="success">
      <div class="p-4">
        <p>这是使用成功背景的 MacWindow 组件。</p>
        <p>背景色：成功主题背景，适合成功状态。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 警告背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">警告背景</h3>
    <MacWindow title="警告背景窗口" height="h-48" bgType="warning">
      <div class="p-4">
        <p>这是使用警告背景的 MacWindow 组件。</p>
        <p>背景色：警告主题背景，适合警告信息。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 错误背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">错误背景</h3>
    <MacWindow title="错误背景窗口" height="h-48" bgType="error">
      <div class="p-4">
        <p>这是使用错误背景的 MacWindow 组件。</p>
        <p>背景色：错误主题背景，适合错误信息。</p>
      </div>
    </MacWindow>
  </div>
</div>
