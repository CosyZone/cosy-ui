---
/**
 * @component MacWindow BgTypes Example
 * @description 展示 MacWindow 组件的不同背景色类型
 */

import { MacWindow } from '@coffic/cosy-ui';
import type { MacWindowBgType } from '@coffic/cosy-ui';
---

<div class="space-y-8">
  <!-- 默认背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">默认背景</h3>
    <MacWindow title="默认背景窗口" height="h-48">
      <div class="p-4">
        <p>这是使用默认背景色的 MacWindow 组件。</p>
        <p>背景色：浅色主题，适合大多数场景。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 浅色背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">浅色背景</h3>
    <MacWindow title="浅色背景窗口" height="h-48" bgType="light">
      <div class="p-4">
        <p>这是使用浅色背景的 MacWindow 组件。</p>
        <p>背景色：明亮的白色背景，适合文档编辑。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 深色背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">深色背景</h3>
    <MacWindow title="深色背景窗口" height="h-48" bgType="dark">
      <div class="p-4">
        <p>这是使用深色背景的 MacWindow 组件。</p>
        <p>背景色：深色主题，适合夜间使用。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 蓝色背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">蓝色背景</h3>
    <MacWindow title="蓝色背景窗口" height="h-48" bgType="blue">
      <div class="p-4">
        <p>这是使用蓝色背景的 MacWindow 组件。</p>
        <p>背景色：蓝色主题，适合科技类应用。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 绿色背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">绿色背景</h3>
    <MacWindow title="绿色背景窗口" height="h-48" bgType="green">
      <div class="p-4">
        <p>这是使用绿色背景的 MacWindow 组件。</p>
        <p>背景色：绿色主题，适合自然类应用。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 紫色背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">紫色背景</h3>
    <MacWindow title="紫色背景窗口" height="h-48" bgType="purple">
      <div class="p-4">
        <p>这是使用紫色背景的 MacWindow 组件。</p>
        <p>背景色：紫色主题，适合创意类应用。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 橙色背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">橙色背景</h3>
    <MacWindow title="橙色背景窗口" height="h-48" bgType="orange">
      <div class="p-4">
        <p>这是使用橙色背景的 MacWindow 组件。</p>
        <p>背景色：橙色主题，适合活力类应用。</p>
      </div>
    </MacWindow>
  </div>

  <!-- 红色背景 -->
  <div>
    <h3 class="text-lg font-semibold mb-4">红色背景</h3>
    <MacWindow title="红色背景窗口" height="h-48" bgType="red">
      <div class="p-4">
        <p>这是使用红色背景的 MacWindow 组件。</p>
        <p>背景色：红色主题，适合警告类应用。</p>
      </div>
    </MacWindow>
  </div>
</div>
