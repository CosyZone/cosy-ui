---

import { AppLayout } from "@coffic/cosy-ui";
import {
	defaultFooterConfig,
	defaultHeaderConfig,
	defaultMainContentConfig,
	defaultMetaConfig,
	defaultSidebarItems,
} from "./shared-config.ts";

const metaConfig = {
	...defaultMetaConfig,
	title: "自定义样式示例",
	description: "展示如何通过 class 属性自定义样式",
	keywords: "AppLayout, 自定义样式, class",
};
---

<AppLayout
  metaConfig={metaConfig}
  sidebarConfig={{
    sidebarItems: defaultSidebarItems,
  }}
  headerConfig={defaultHeaderConfig}
  footerConfig={defaultFooterConfig}
  mainContentConfig={defaultMainContentConfig}
  class="cosy:border-2 cosy:border-primary cosy:rounded-lg">
  <div class="cosy:p-6">
    <h1 class="cosy:text-2xl cosy:font-bold cosy:mb-4">自定义样式示例</h1>
    <p class="cosy:text-gray-600">
      这个示例展示了如何通过 class 属性为 AppLayout 组件添加自定义样式。
    </p>
    <p class="cosy:text-gray-600 cosy:mt-2">
      可以看到整个布局容器添加了边框和圆角样式。
    </p>
  </div>
</AppLayout>
