---
/**
 * @component Image3ErrorTooMany
 * @description Image3 组件图片数量过多错误示例
 */
import { Image3, getProductImage } from '@coffic/cosy-ui';

const images = [
  {
    src: getProductImage({ width: 200, height: 300, tag: 'error-many-1' }),
    alt: '图片 1',
    width: 200,
    height: 300,
  },
  {
    src: getProductImage({ width: 200, height: 300, tag: 'error-many-2' }),
    alt: '图片 2',
    width: 200,
    height: 300,
  },
  {
    src: getProductImage({ width: 200, height: 300, tag: 'error-many-3' }),
    alt: '图片 3',
    width: 200,
    height: 300,
  },
  {
    src: getProductImage({ width: 200, height: 300, tag: 'error-many-4' }),
    alt: '图片 4',
    width: 200,
    height: 300,
  },
  // 故意提供4张图片，触发错误视图
] as any;
---

<Image3 images={images} />
