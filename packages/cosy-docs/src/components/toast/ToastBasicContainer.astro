---

/**
 * @component ToastBasicContainer
 * @description Toast 基础示例的容器组件，使用 CodeContainer 的多标签系统展示不同用法
 */

import { CodeContainer, ToastContainer } from "@coffic/cosy-ui";
import ToastControl from "./ToastControl.astro";
import ToastControlCode from "./ToastControl.astro?raw";
import ToastDuration from "./ToastDuration.astro";
import ToastDurationCode from "./ToastDuration.astro?raw";
import ToastRealWorld from "./ToastRealWorld.astro";
import ToastRealWorldCode from "./ToastRealWorld.astro?raw";
import ToastTypes from "./ToastTypes.astro";
import ToastTypesCode from "./ToastTypes.astro?raw";
---

<!-- 全局 Toast 容器 -->
<ToastContainer />

<!-- 使用说明 -->
<div class="cosy:alert cosy:alert-info cosy:mb-4">
  <div class="cosy:flex cosy:gap-2">
    <svg
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      class="cosy:w-6 cosy:h-6 cosy:stroke-current cosy:shrink-0">
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
    </svg>
    <div>
      <p class="cosy:font-medium">重要提示</p>
      <p class="cosy:text-sm">
        在实际应用中，请确保将 <code class="cosy:badge cosy:badge-ghost"
          >&lt;ToastContainer /&gt;</code
        > 放置在应用的根组件或布局组件中，这样 Toast 通知才能在任何页面正常工作。
      </p>
    </div>
  </div>
</div>

<CodeContainer
  titles={['消息类型', '持续时间', '手动控制', '实际场景']}
  descriptions={[
    '展示四种不同类型的 Toast 通知：info、success、warning、error',
    '演示如何自定义 Toast 的显示时长，从快速提示到持久显示',
    '展示手动控制 Toast 的各种操作：显示、关闭、批量管理',
    '模拟真实应用场景：表单保存、文件上传、系统通知等',
  ]}
  codes={[
    ToastTypesCode,
    ToastDurationCode,
    ToastControlCode,
    ToastRealWorldCode,
  ]}>
  <div id="tab-1">
    <ToastTypes />
  </div>

  <div id="tab-2">
    <ToastDuration />
  </div>

  <div id="tab-3">
    <ToastControl />
  </div>

  <div id="tab-4">
    <ToastRealWorld />
  </div>
</CodeContainer>
