---
import { AlertFactory } from '@coffic/cosy-ui';

const alertElement = await AlertFactory.withType('success')
    .withShowIcon(false)
    .withTitle('成功提示（无图标）')
    .build();
---

<Fragment set:html={alertElement} />
