---
import { AlertFactory } from '@coffic/cosy-ui';

const alertElement = await AlertFactory.withType('error')
	.withVariant('soft')
	.withTitle('Soft 错误')
	.build();
---

<Fragment set:html={alertElement} />
