---
import { AlertFactory as AlertFactoryClass } from '@coffic/cosy-ui';

// 创建 Alert 组件实例
const alertElement = await AlertFactoryClass.withTitle('Hello World').build();
---

<Fragment set:html={alertElement} />
