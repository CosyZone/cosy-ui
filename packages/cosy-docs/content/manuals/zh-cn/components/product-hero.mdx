---
title: 'ProductHero'
folder: false
order: 16.6
tags:
- astro
- product
- hero
- apple-style
- display
---

import { ProductHero } from '@coffic/cosy-ui';
import { ProductHeroPackage } from '@/components/product-hero/index';

# {frontmatter.title}

## 简介

ProductHero 组件是 ProductHeroItem 的容器组件，用于展示多个产品的详细信息。采用苹果风格的大图展示设计，支持左右交替布局，提供优雅的产品展示体验。适用于产品展示页面、产品详情页等场景。

组件特性：

- 大图展示 - 突出产品图片，采用高质量大图展示
- 交替布局 - 自动左右交替布局，增加视觉层次
- 信息层次 - 清晰的信息层次：标签、标题、描述、价格、按钮
- 苹果风格 - 采用苹果官网的简洁、优雅设计风格
- 响应式设计 - 在不同设备上都能良好展示

## 案例

<ProductHeroPackage.Example />

## Props

### backgrounds

背景色类型数组，使用预定义的背景色类型，如 'primary', 'base-200', 'gradient-sky' 等。组件会循环使用这些背景色，如果只有一个背景色则所有产品项都使用该背景色。

<ProductHeroPackage.Backgrounds />

### buyNowText

购买按钮文本，默认为"立即购买"。

<ProductHeroPackage.BuyNowText />

### class

自定义CSS类，可用于覆盖默认样式。

<ProductHeroPackage.Class />

### learnMoreText

了解更多按钮文本，默认为"了解更多"。

<ProductHeroPackage.LearnMoreText />

### newLabelText

新品标签文本，默认为"新品"。

<ProductHeroPackage.NewLabelText />

### onBuyNow

购买按钮点击事件回调函数。

<ProductHeroPackage.OnBuyNow />

### onLearnMore

了解更多按钮点击事件回调函数。

<ProductHeroPackage.OnLearnMore />

### products

产品数据数组，每个产品包含以下属性：

- `name`: 产品名称
- `description`: 产品描述
- `image`: 产品图片
- `price`: 产品价格
- `isNew`: 是否为新品（可选）

<ProductHeroPackage.Products />

