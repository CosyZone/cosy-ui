---
title: "PlaceHolder"
folder: false
order: 16
---

import { PlaceHolder } from '@coffic/cosy-ui';
import { PlaceHolderPackage } from '@/components/placeholder/index';

# {frontmatter.title}

## 简介

PlaceHolder 组件用于在布局中占用指定的空间，类似于 SwiftUI 的 Spacer。可以独立设置组件的宽度和高度，支持不同的尺寸选项和背景色配置。

## 特性

- **空间占用** - 在布局中占用指定的空间
- **灵活性** - 支持独立控制宽度和高度
- **一致性** - 与设计系统保持一致的尺寸规范
- **响应式** - 在不同屏幕尺寸下保持良好的布局效果
- **可视化** - 支持背景色配置，便于调试和设计

## background

背景色类名，可选值：base-100、base-200、base-300、primary、secondary、accent、info、success、warning、error。

<PlaceHolderPackage.Backgrounds />

## class

自定义 CSS 类名，用于添加额外的样式。

<PlaceHolderPackage.BasicTheme />

## height

高度尺寸，可选值：none、xs、sm、md、lg、xl、2xl、3xl、4xl、5xl、6xl、full。

<PlaceHolderPackage.Sizes />

## padding

内边距大小，可选值：none、xs、sm、md、lg、xl。

<PlaceHolderPackage.Padding />

## width

宽度尺寸，可选值：none、xs、sm、md、lg、xl、2xl、3xl、4xl、5xl、6xl、full。

<PlaceHolderPackage.Sizes />

## 基础用法

使用 `width` 和 `height` 属性设置占位符的宽度和高度，使用 `padding` 属性设置内边距：

<PlaceHolderPackage.Basic />
