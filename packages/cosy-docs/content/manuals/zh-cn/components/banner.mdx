---
title: 'Banner'
folder: false
order: 2.3
---

import { Banner } from '@coffic/cosy-ui';
import { BannerPackage } from '@/components/banner/index';

# {frontmatter.title}

## 简介

Banner 组件用于在页面顶部显示醒目的横幅信息，通常用于展示重要的标语、公告或营销信息。

组件特性：
- 支持多种预设颜色（primary/secondary/success/warning/error/info）
- 可自定义样式和背景
- 支持多种动画效果选择
- 响应式设计，适配不同屏幕尺寸

## animation

动画效果类型，可选值：`none`（无动画）、`fade`（仅淡入）、`slide`（仅上滑）、`both`（淡入和上滑）。

<BannerPackage.Animations />

## bgColor

背景颜色，支持基础颜色和渐变色彩。

<BannerPackage.Colors />

## class

自定义 CSS 类名，用于覆盖默认样式。

<BannerPackage.CustomStyle />

## style

自定义 CSS 样式，用于覆盖默认样式。

<BannerPackage.CustomStyle />

## textColor

文本颜色，默认根据背景色自动设置，可手动指定颜色值。

<BannerPackage.Basic />

## 插槽

- `default`：Banner 中显示的文本内容
