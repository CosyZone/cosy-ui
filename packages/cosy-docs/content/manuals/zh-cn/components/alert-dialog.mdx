---
title: 'AlertDialog'
folder: false
order: 1.5
---

import { AlertDialog } from '@coffic/cosy-ui';
import { ComponentPackage } from '@/components/alert-dialog/index';

# {frontmatter.title}

## 简介

AlertDialog 组件用于显示简单的确认对话框，支持国际化，带有淡入淡出动画效果。

组件特性：

- 支持中英文国际化
- 带有淡入淡出动画效果
- 点击背景或按钮可关闭
- 响应式设计，适配不同屏幕尺寸
- 支持自定义对话框 ID
- 使用 daisyUI 样式，与整体设计风格一致

## 基础用法

最简单的确认对话框：

<ComponentPackage.ComponentContainers.Basic />

## 英文对话框

通过 `lang="en"` 属性可以显示英文对话框：

<ComponentPackage.ComponentContainers.English />

## API 参考

### Props

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| message | string | - | 对话框显示的消息内容 |
| lang | 'zh-cn' \| 'en' | 'zh-cn' | 语言设置，影响按钮文本 |
| id | string | 'alert-dialog' | 对话框的唯一标识符 |

### 使用说明

1. **显示对话框**：通过 `showAlertDialog(dialogId)` 函数显示对话框
2. **关闭对话框**：点击背景遮罩或确认按钮可关闭对话框
3. **多语言支持**：通过 `lang` 属性切换中英文界面
4. **自定义 ID**：通过 `id` 属性设置对话框的唯一标识符

### 示例代码

```astro
---
import { AlertDialog } from '@coffic/cosy-ui';
---

<button 
  style="
    background-color: #3b82f6;
    color: white;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 0.375rem;
    cursor: pointer;
  "
  onclick="showAlertDialog('my-dialog')">
  显示对话框
</button>

<AlertDialog 
  message="确认要执行此操作吗？" 
  id="my-dialog"
/>
``` 
