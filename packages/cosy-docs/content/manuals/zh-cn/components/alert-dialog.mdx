---
title: 'AlertDialog'
folder: false
order: 1.2
---

import { AlertDialog } from '@coffic/cosy-ui';
import { AlertDialogPackage } from '@/components/alert-dialog/index';

# {frontmatter.title}

## 简介

AlertDialog 组件用于显示简单的确认对话框，支持国际化，带有淡入淡出动画效果。

## 案例

<AlertDialogPackage.Basic />

## Props

### class

自定义 CSS 类名，用于覆盖默认样式。

<AlertDialogPackage.CustomStyle />

### id

对话框的唯一标识符，用于通过 JavaScript 控制对话框的显示和隐藏（仅 Astro 版本）。

<AlertDialogPackage.Basic />

### lang

语言设置，影响按钮文本，支持中文（zh）和英文（en）两种语言。

<AlertDialogPackage.English />

### message

对话框显示的消息内容，支持任意文本内容。

<AlertDialogPackage.Basic />

### modelValue

控制对话框显示状态，支持 v-model 双向绑定（仅 Vue 版本）。

<AlertDialogPackage.Basic />

## Slots

### default

无插槽内容，所有内容通过 message 属性传递。
