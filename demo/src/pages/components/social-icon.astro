---
import Layout from '../../layouts/DemoLayout.astro';
import { SocialIcon as SocialIconComponent, CodeExample } from '@coffic/cosy-ui';

const title = "社交图标 - SocialIcon";
const description = "用于展示社交媒体平台图标的组件";

// 支持的社交平台列表
const platforms = [
  'github',
  'twitter',
  'linkedin',
  'weibo',
  'wechat',
  'zhihu',
  'bilibili',
  'youtube',
  'discord'
];

// 不同尺寸的类名
const sizes = [
  { name: '小', class: 'w-4 h-4' },
  { name: '中', class: 'w-6 h-6' },
  { name: '大', class: 'w-8 h-8' },
  { name: '超大', class: 'w-10 h-10' }
];

// 不同颜色的类名和标签
const colors = [
  { name: '默认', class: 'text-gray-700 dark:text-gray-300' },
  { name: '主要', class: 'text-blue-600 dark:text-blue-400' },
  { name: '成功', class: 'text-green-600 dark:text-green-400' },
  { name: '警告', class: 'text-yellow-600 dark:text-yellow-400' },
  { name: '危险', class: 'text-red-600 dark:text-red-400' },
  { name: '信息', class: 'text-purple-600 dark:text-purple-400' }
];

// 基本用法示例代码
const basicUsageCode = `
import { SocialIcon } from '@coffic/cosy-ui';

<SocialIcon platform="github" />
<SocialIcon platform="twitter" />
<SocialIcon platform="linkedin" />
`.trim();

// 自定义尺寸示例代码
const customSizeCode = `
<SocialIcon platform="github" class="w-4 h-4" />
<SocialIcon platform="github" class="w-6 h-6" />
<SocialIcon platform="github" class="w-8 h-8" />
<SocialIcon platform="github" class="w-10 h-10" />
`.trim();

// 自定义颜色示例代码
const customColorCode = `
<SocialIcon platform="github" class="text-gray-700 dark:text-gray-300" />
<SocialIcon platform="github" class="text-blue-600 dark:text-blue-400" />
<SocialIcon platform="github" class="text-green-600 dark:text-green-400" />
<SocialIcon platform="github" class="text-red-600 dark:text-red-400" />
`.trim();

// 在链接中使用示例代码
const linkUsageCode = `
<a href="https://github.com/username" class="inline-flex items-center gap-2 text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300">
  <SocialIcon platform="github" class="w-5 h-5" />
  <span>访问我的 GitHub</span>
</a>
`.trim();

// 暗黑模式适配示例代码
const darkModeCode = `
<!-- 自动适应暗黑模式 -->
<div class="flex gap-4">
  <SocialIcon platform="github" class="text-gray-700 dark:text-gray-300" />
  <SocialIcon platform="twitter" class="text-gray-700 dark:text-gray-300" />
  <SocialIcon platform="linkedin" class="text-gray-700 dark:text-gray-300" />
</div>
`.trim();
---

<Layout title={title}>
  <main class="container mx-auto p-4 space-y-4">
    <h1 class="text-2xl font-bold mb-6">{title}</h1>
    <p class="mb-8">{description}</p>

    <!-- 组件介绍 -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">组件介绍</h2>
      <div class="prose dark:prose-invert max-w-none">
        <p>
          <code>SocialIcon</code> 组件用于展示各种社交媒体平台的图标。该组件支持多种常见的社交媒体平台，
          并且可以自定义大小和颜色。组件已适配暗黑模式，会根据用户系统设置自动切换显示效果。
        </p>
        <h3>特点</h3>
        <ul>
          <li>支持多种常见社交媒体平台</li>
          <li>可自定义大小和颜色</li>
          <li>使用 SVG 图标，确保在任何分辨率下都清晰显示</li>
          <li>适配暗黑模式，自动根据系统设置切换显示效果</li>
          <li>符合无障碍标准，包含适当的 ARIA 属性</li>
        </ul>
      </div>
    </section>

    <!-- 基本用法 -->
    <CodeExample 
      title="基本用法" 
      description="使用 platform 属性指定要显示的社交媒体平台图标。"
      code={basicUsageCode}
    >
      <div class="flex flex-wrap gap-6 items-center">
        {platforms.map(platform => (
          <div class="flex flex-col items-center gap-2">
            <SocialIconComponent platform={platform} class="w-8 h-8" />
            <span class="text-sm">{platform}</span>
          </div>
        ))}
      </div>
    </CodeExample>

    <!-- 自定义尺寸 -->
    <CodeExample 
      title="自定义尺寸" 
      description="通过 class 属性设置图标的尺寸。"
      code={customSizeCode}
    >
      <div class="flex flex-wrap gap-6 items-end">
        {sizes.map(size => (
          <div class="flex flex-col items-center gap-2">
            <SocialIconComponent platform="github" class={size.class} />
            <span class="text-sm">{size.name}</span>
          </div>
        ))}
      </div>
    </CodeExample>

    <!-- 自定义颜色 -->
    <CodeExample 
      title="自定义颜色" 
      description="通过 class 属性设置图标的颜色。图标使用 currentColor，因此可以通过文本颜色类来控制图标颜色。"
      code={customColorCode}
    >
      <div class="flex flex-wrap gap-6 items-center">
        {colors.map(color => (
          <div class="flex flex-col items-center gap-2">
            <SocialIconComponent platform="github" class={`w-8 h-8 ${color.class}`} />
            <span class="text-sm">{color.name}</span>
          </div>
        ))}
      </div>
    </CodeExample>

    <!-- 在链接中使用 -->
    <CodeExample 
      title="在链接中使用" 
      description="社交图标通常与链接一起使用，下面是一个示例："
      code={linkUsageCode}
    >
      <a href="#" class="inline-flex items-center gap-2 text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300">
        <SocialIconComponent platform="github" class="w-5 h-5" />
        <span>访问我的 GitHub</span>
      </a>
    </CodeExample>

    <!-- 暗黑模式适配 -->
    <CodeExample 
      title="暗黑模式适配" 
      description="SocialIcon 组件已经适配暗黑模式，会根据用户系统设置自动切换显示效果。在暗黑模式下，图标会自动调整亮度以提高可见性。"
      code={darkModeCode}
    >
      <div class="flex gap-4">
        <SocialIconComponent platform="github" class="w-6 h-6 text-gray-700 dark:text-gray-300" />
        <SocialIconComponent platform="twitter" class="w-6 h-6 text-gray-700 dark:text-gray-300" />
        <SocialIconComponent platform="linkedin" class="w-6 h-6 text-gray-700 dark:text-gray-300" />
      </div>
      
      <div class="mt-4 p-4 bg-yellow-50 dark:bg-yellow-900/30 border-l-4 border-yellow-500 text-yellow-700 dark:text-yellow-200">
        <p>
          <strong>提示：</strong> 您可以通过系统设置切换暗黑/亮色模式来查看效果变化。
          组件会自动适应系统的颜色方案设置。
        </p>
      </div>
    </CodeExample>

    <!-- 属性说明 -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">属性说明</h2>
      
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
          <thead class="bg-gray-50 dark:bg-gray-800">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">属性名</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">类型</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">默认值</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">说明</th>
            </tr>
          </thead>
          <tbody class="bg-white dark:bg-gray-900 divide-y divide-gray-200 dark:divide-gray-800">
            <tr>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">platform</td>
              <td class="px-6 py-4 whitespace-nowrap text-sm">string</td>
              <td class="px-6 py-4 whitespace-nowrap text-sm">'default'</td>
              <td class="px-6 py-4 text-sm">
                社交媒体平台名称。支持的值：
                <code>github</code>, <code>twitter</code>, <code>linkedin</code>, 
                <code>weibo</code>, <code>wechat</code>, <code>zhihu</code>, 
                <code>bilibili</code>, <code>youtube</code>, <code>discord</code>
              </td>
            </tr>
            <tr>
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">class</td>
              <td class="px-6 py-4 whitespace-nowrap text-sm">string</td>
              <td class="px-6 py-4 whitespace-nowrap text-sm">'w-4 h-4'</td>
              <td class="px-6 py-4 text-sm">自定义类名，可用于设置图标的大小和颜色</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <!-- 无障碍说明 -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">无障碍说明</h2>
      <div class="prose dark:prose-invert max-w-none">
        <p>
          <code>SocialIcon</code> 组件已经包含了适当的无障碍属性：
        </p>
        <ul>
          <li>使用 <code>role="img"</code> 确保屏幕阅读器将其识别为图像</li>
          <li>包含 <code>aria-label</code> 属性，提供图标的文本描述</li>
          <li>包含 <code>title</code> 元素，当用户悬停在图标上时显示提示文本</li>
        </ul>
        <p>
          当在链接中使用社交图标时，请确保链接具有描述性的文本内容，以便屏幕阅读器用户理解链接的目的。
        </p>
      </div>
    </section>
  </main>
</Layout>

<style>
  /* 确保代码块在暗黑模式下有足够的对比度 */
  :global(.dark) code {
    color: #e0e0e0;
    background-color: rgba(255, 255, 255, 0.1);
  }
</style> 