---
import Layout from '../../layouts/Layout.astro';
import { CodeBlock } from '@coffic/cosy-ui';

const basicExample = `<CodeBlock
  code="console.log('Hello, World!');"
  lang="javascript"
  title="基础示例"
/>`;

const htmlExample = `<div class="alert alert-info">
  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-6 h-6">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
  </svg>
  <span>这是一个信息提示框</span>
</div>`;

const tsExample = `interface User {
  id: number;
  name: string;
  email: string;
  isActive: boolean;
}

function createUser(data: Partial<User>): User {
  return {
    id: Math.random(),
    name: '',
    email: '',
    isActive: true,
    ...data
  };
}`;
---

<Layout title="Code 代码 - Cosy UI">
  <div class="prose max-w-none">
    <h1>Code 代码</h1>
    <p>用于展示代码片段的组件，支持语法高亮和代码复制功能。</p>

    <h2>基础用法</h2>
    <p>最简单的代码展示：</p>
    
    <CodeBlock
      code={basicExample}
      lang="astro"
      title="基础示例"
    />

    <div class="my-8">
      <CodeBlock
        code="console.log('Hello, World!');"
        lang="javascript"
        title="运行结果"
      />
    </div>

    <h2>HTML 代码示例</h2>
    <p>展示 HTML 代码，包含 DaisyUI 组件：</p>

    <CodeBlock
      code={htmlExample}
      lang="html"
      title="HTML 示例"
    />

    <div class="my-8 not-prose" set:html={htmlExample} />

    <h2>TypeScript 代码示例</h2>
    <p>展示 TypeScript 代码，包含接口定义和函数：</p>

    <CodeBlock
      code={tsExample}
      lang="typescript"
      title="TypeScript 示例"
    />

    <h2>不显示行号</h2>
    <p>可以通过 <code>showLineNumbers</code> 属性控制是否显示行号：</p>

    <CodeBlock
      code="const greeting = 'Hello, World!';"
      lang="javascript"
      showLineNumbers={false}
    />

    <h2>API</h2>
    <CodeBlock
      code={`interface Props {
  code: string;      // 要显示的代码字符串
  lang?: string;     // 代码语言，用于语法高亮
  title?: string;    // 代码块标题
  showLineNumbers?: boolean; // 是否显示行号，默认为 true
}`}
      lang="typescript"
      title="组件属性"
    />

    <h3>支持的语言</h3>
    <p>目前支持以下常用编程语言的语法高亮：</p>
    <ul>
      <li>HTML</li>
      <li>CSS</li>
      <li>JavaScript</li>
      <li>TypeScript</li>
      <li>JSX/TSX</li>
      <li>Vue</li>
      <li>Astro</li>
      <li>Markdown</li>
      <li>JSON</li>
      <li>YAML</li>
      <li>Bash</li>
      <li>Python</li>
      <li>Java</li>
      <li>C/C++</li>
      <li>Go</li>
      <li>Rust</li>
    </ul>
  </div>
</Layout> 