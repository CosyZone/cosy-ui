---
import Layout from '../../layouts/DemoLayout.astro';
import { Text as TextComponent, CodeExample } from '@coffic/cosy-ui';
---

<Layout title="Text 组件示例">
  <main class="container mx-auto p-4 space-y-4">
    <h1 class="text-2xl font-bold mb-6">Text 组件示例</h1>

    <div class="space-y-8">
      <!-- 基础用法 -->
      <CodeExample 
        title="基础用法" 
        description="最基本的文本组件"
        code={`<Text>这是一段普通文本，默认使用 p 标签渲染。</Text>`}
      >
        <TextComponent>这是一段普通文本，默认使用 p 标签渲染。</TextComponent>
      </CodeExample>

      <!-- 不同大小 -->
      <CodeExample 
        title="不同大小" 
        description="Text 组件支持多种大小"
        code={`<Text size="xs">这是超小号文本</Text>

<Text size="sm">这是小号文本</Text>

<Text size="md">这是中号文本（默认）</Text>

<Text size="lg">这是大号文本</Text>

<Text size="xl">这是超大号文本</Text>`}
      >
        <div class="space-y-2">
          <TextComponent size="xs">这是超小号文本</TextComponent>
          <TextComponent size="sm">这是小号文本</TextComponent>
          <TextComponent size="md">这是中号文本（默认）</TextComponent>
          <TextComponent size="lg">这是大号文本</TextComponent>
          <TextComponent size="xl">这是超大号文本</TextComponent>
        </div>
      </CodeExample>

      <!-- 不同颜色 -->
      <CodeExample 
        title="不同颜色" 
        description="Text 组件支持多种颜色选项"
        code={`<Text color="default">这是默认颜色文本</Text>

<Text color="primary">这是主要颜色文本</Text>

<Text color="secondary">这是次要颜色文本</Text>

<Text color="accent">这是强调色文本</Text>

<Text color="muted">这是柔和色文本</Text>`}
      >
        <div class="space-y-2">
          <TextComponent color="default">这是默认颜色文本</TextComponent>
          <TextComponent color="primary">这是主要颜色文本</TextComponent>
          <TextComponent color="secondary">这是次要颜色文本</TextComponent>
          <TextComponent color="accent">这是强调色文本</TextComponent>
          <TextComponent color="muted">这是柔和色文本</TextComponent>
        </div>
      </CodeExample>

      <!-- 字体粗细 -->
      <CodeExample 
        title="字体粗细" 
        description="可以设置不同的字体粗细"
        code={`<Text weight="light">这是细体文本</Text>

<Text weight="normal">这是常规文本（默认）</Text>

<Text weight="medium">这是中等粗文本</Text>

<Text weight="semibold">这是半粗体文本</Text>

<Text weight="bold">这是粗体文本</Text>`}
      >
        <div class="space-y-2">
          <TextComponent weight="light">这是细体文本</TextComponent>
          <TextComponent weight="normal">这是常规文本（默认）</TextComponent>
          <TextComponent weight="medium">这是中等粗文本</TextComponent>
          <TextComponent weight="semibold">这是半粗体文本</TextComponent>
          <TextComponent weight="bold">这是粗体文本</TextComponent>
        </div>
      </CodeExample>

      <!-- 文本对齐 -->
      <CodeExample 
        title="文本对齐" 
        description="可以设置文本的对齐方式"
        code={`<Text align="left">这是左对齐文本（默认）</Text>

<Text align="center">这是居中对齐文本</Text>

<Text align="right">这是右对齐文本</Text>

<Text align="justify">这是两端对齐文本。这是一段较长的文本，用于演示两端对齐的效果。两端对齐会使文本的左右边缘都对齐，通过调整单词之间的间距来实现。</Text>`}
      >
        <div class="space-y-2">
          <TextComponent align="left">这是左对齐文本（默认）</TextComponent>
          <TextComponent align="center">这是居中对齐文本</TextComponent>
          <TextComponent align="right">这是右对齐文本</TextComponent>
          <TextComponent align="justify">这是两端对齐文本。这是一段较长的文本，用于演示两端对齐的效果。两端对齐会使文本的左右边缘都对齐，通过调整单词之间的间距来实现。</TextComponent>
        </div>
      </CodeExample>

      <!-- 文本样式 -->
      <CodeExample 
        title="文本样式" 
        description="可以设置斜体和下划线"
        code={`<Text italic>这是斜体文本</Text>

<Text underline>这是带下划线的文本</Text>

<Text italic underline>这是斜体且带下划线的文本</Text>`}
      >
        <div class="space-y-2">
          <TextComponent italic>这是斜体文本</TextComponent>
          <TextComponent underline>这是带下划线的文本</TextComponent>
          <TextComponent italic underline>这是斜体且带下划线的文本</TextComponent>
        </div>
      </CodeExample>

      <!-- 文本截断 -->
      <CodeExample 
        title="文本截断" 
        description="可以设置文本截断并显示省略号"
        code={`<Text truncate>这是一段很长的文本，将会被截断并显示省略号。Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam euismod, nisl eget aliquam ultricies, nunc nisl aliquet nunc, quis aliquam nisl nunc quis nisl.</Text>`}
      >
        <div class="max-w-md">
          <TextComponent truncate>这是一段很长的文本，将会被截断并显示省略号。Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam euismod, nisl eget aliquam ultricies, nunc nisl aliquet nunc, quis aliquam nisl nunc quis nisl.</TextComponent>
        </div>
      </CodeExample>

      <!-- 自定义元素 -->
      <CodeExample 
        title="自定义元素" 
        description="可以指定要渲染的HTML元素"
        code={`<Text as="span">这是一个 span 元素</Text>

<Text as="div">这是一个 div 元素</Text>

<Text as="label">这是一个 label 元素</Text>`}
      >
        <div class="space-y-2">
          <TextComponent as="span">这是一个 span 元素</TextComponent>
          <TextComponent as="div">这是一个 div 元素</TextComponent>
          <TextComponent as="label">这是一个 label 元素</TextComponent>
        </div>
      </CodeExample>

      <!-- 组合使用 -->
      <CodeExample 
        title="组合使用" 
        description="组合多种属性创建自定义文本样式"
        code={`<Text 
  size="lg" 
  color="primary" 
  weight="bold" 
  align="center"
  italic
  class="p-4 bg-gray-100 rounded-lg shadow-sm"
>
  这是一段综合样式的文本
</Text>`}
      >
        <TextComponent 
          size="lg" 
          color="primary" 
          weight="bold" 
          align="center"
          italic
          class="p-4 bg-gray-100 rounded-lg shadow-sm"
        >
          这是一段综合样式的文本
        </TextComponent>
      </CodeExample>
    </div>
  </main>
</Layout> 
