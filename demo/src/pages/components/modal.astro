---
import { Modal as ModalComponent } from '@coffic/cosy-ui';
import Layout from '../../layouts/DemoLayout.astro';
---

<Layout title="Modal Component">
    <main class="container mx-auto px-4 py-8">
        <h1 class="text-2xl font-bold mb-8">Modal 组件示例</h1>

        <div class="flex flex-col gap-8">
            <!-- 基础示例 -->
            <div class="card bg-base-200 p-4">
                <h2 class="text-xl font-semibold mb-4">基础示例</h2>
                <button class="btn btn-primary" data-modal-target="basic-modal">打开模态框</button>

                <ModalComponent id="basic-modal" title="基础模态框">
                    <p>这是一个基础的模态框示例。它包含了标题和关闭按钮。</p>
                </ModalComponent>
            </div>

            <!-- 带操作按钮的示例 -->
            <div class="card bg-base-200 p-4">
                <h2 class="text-xl font-semibold mb-4">带操作按钮的模态框</h2>
                <button class="btn btn-secondary" data-modal-target="action-modal">打开带操作的模态框</button>

                <ModalComponent id="action-modal" title="确认操作">
                    <p>您确定要执行此操作吗？</p>
                    <slot name="actions">
                        <form method="dialog" class="flex gap-2">
                            <button class="btn">取消</button>
                            <button class="btn btn-primary">确认</button>
                        </form>
                    </slot>
                </ModalComponent>
            </div>

            <!-- 自定义样式示例 -->
            <div class="card bg-base-200 p-4">
                <h2 class="text-xl font-semibold mb-4">自定义样式的模态框</h2>
                <button class="btn btn-accent" data-modal-target="custom-modal">打开自定义模态框</button>

                <ModalComponent
                    id="custom-modal"
                    title="自定义样式"
                    class="bg-gradient-to-br from-purple-100 to-pink-100 dark:from-purple-900 dark:to-pink-900"
                >
                    <div class="prose dark:prose-invert">
                        <p>这个模态框使用了自定义的背景样式。</p>
                        <ul>
                            <li>支持深色模式</li>
                            <li>使用渐变背景</li>
                            <li>可以添加任何 Tailwind 类</li>
                        </ul>
                    </div>
                </ModalComponent>
            </div>
        </div>
    </main>
</Layout> 