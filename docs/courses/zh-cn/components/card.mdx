---
title: 'Card'
description: '用于在页面中展示相关内容的容器，通常包含标题、描述和可选的图片'
folder: false
order: 2
---

import { Card, CardPackage } from '@cosy/index';

## 简介

Card组件用于在页面中展示相关内容的容器，通常包含标题、描述和可选的图片。组件设计简洁大方，具有平滑的动画效果和交互反馈，适合展示产品、文章或功能介绍等内容。

组件特性：
- 支持图片展示，自动适配尺寸
- 可设置为可点击的链接卡片
- 提供紧凑模式，适应不同空间需求
- 平滑的悬停动画效果
- 响应式设计，适配不同屏幕尺寸

## 基础用法

最简单的卡片展示：

<CardPackage.CardContainers.Basic/>

## 带副标题的卡片

通过subtitle属性添加描述信息：

<CardPackage.CardContainers.WithSubtitle/>

## 带图片的卡片

通过imageUrl属性添加顶部图片：

<CardPackage.CardContainers.WithImage/>

## 可点击的卡片

通过href属性将卡片设置为链接：

<CardPackage.CardContainers.Clickable/>

## 紧凑模式

使用compact属性减少内边距：

<CardPackage.CardContainers.Compact/>

## 自定义样式

通过class属性自定义卡片样式：

<CardPackage.CardContainers.CustomStyle/> 
