---
title: 'MacWindow'
description: 'macOS 风格的窗口组件，用于创建模拟桌面应用界面'
folder: false
order: 2
---

import { CodeExample } from '@cosy/index';
import { MacWindow, MacWindowExamples, MacWindowExampleCodes } from '@cosy/index_vue';
import { SearchIcon, SettingsIcon, InfoIcon } from '@cosy/index';

## 基础用法

最基本的窗口展示方式，带有标题和窗口控制按钮。

<CodeExample
  code={MacWindowExampleCodes.Basic}
>
  <MacWindowExamples.Basic client:load />
</CodeExample>

## 标签页

传递字符串数组作为标签页。组件内部会自动管理当前激活的标签，点击标签会触发 `onTabClick` 回调，您需要自行维护标签状态。

<CodeExample
  code={MacWindowExampleCodes.WithTabs}
>
  <MacWindowExamples.WithTabs client:load />
</CodeExample>

## 事件处理

通过 `onCloseWindow`、`onMinimizeWindow` 和 `onMaximizeWindow` 属性处理窗口控制按钮的点击事件。还可以使用 `onTabClick` 监听标签切换。

<CodeExample
  code={MacWindowExampleCodes.WithEvents}
>
  <MacWindowExamples.WithEvents client:load />
</CodeExample>

## 工具栏和状态栏

使用 `toolbar` 和 `status` 插槽自定义工具栏和状态栏内容。

<CodeExample
  code={MacWindowExampleCodes.WithToolbar}
>
  <MacWindowExamples.WithToolbar client:load />
</CodeExample>

## 侧边栏

使用 `sidebar` 插槽添加侧边栏内容。

<CodeExample
  code={MacWindowExampleCodes.WithSidebar}
>
  <MacWindowExamples.WithSidebar client:load />
</CodeExample>

## 自定义高度

通过 `height` 属性自定义窗口高度。

<CodeExample
  code={MacWindowExampleCodes.CustomHeight}
>
  <MacWindowExamples.CustomHeight client:load />
</CodeExample>

## 属性

| 属性名 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| height | string | 'h-96' | 窗口高度 |
| title | string | '' | 窗口标题 |
| withShadow | boolean | true | 是否显示阴影效果 |
| tabs | array | [] | 标签页字符串数组，如 ["标签1", "标签2", "标签3"] |
| onCloseWindow | function | null | 点击关闭按钮时调用的函数 |
| onMinimizeWindow | function | null | 点击最小化按钮时调用的函数 |
| onMaximizeWindow | function | null | 点击最大化按钮时调用的函数 |
| onTabClick | function | null | 点击标签页时调用的函数，接收标签页值作为参数 |

## 插槽

| 插槽名 | 说明 |
| --- | --- |
| default | 窗口主要内容 |
| sidebar | 侧边栏内容 |
| toolbar | 工具栏内容，位于标题栏右侧 |
| status | 状态栏内容，位于窗口底部 |
